import type { Metadata } from 'next'

export const metadata: Metadata = {
    title: 'Campaigns | Add Video',
}

export default function AddVideoLayout({ children }: { children: React.ReactNode }) {
    return children
}
